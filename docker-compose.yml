version: '3.8'

services:
  habit_friends_api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - habit_db:/app/habit_friends.db
    environment:
      - DATABASE_URL=sqlite:///./habit_friends.db

volumes:
  habit_db: